const path = require('path');

module.exports = {
    config: path.resolve(__dirname, 'src', 'configs', 'database', 'db-config.js'),
    'migrations-path': path.resolve(__dirname, 'src', 'configs', 'database', 'migrations')
};